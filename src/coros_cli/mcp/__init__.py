"""COROS MCP OAuth integration.

Experimental, read-only client for the official COROS MCP server. Authenticates
via OAuth 2.0 Dynamic Client Registration + Authorization Code Grant with PKCE,
entirely separate from the legacy web/mobile login used by `coros login` /
`coros sleep`.
"""
